# Genius BFF protos

Protobuf / ConnectRPC service definitions for the Genius AI BFF (`genius.*.v1`).

Generate a TypeScript client: see [docs/bff-connect-client.md](../docs/bff-connect-client.md).

```bash
# from repo root
npx buf dep update
npx buf generate
```
